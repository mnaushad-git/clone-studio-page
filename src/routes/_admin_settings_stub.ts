import { createFileRoute } from "@tanstack/react-router";
import { useAdmin, settingsStore, paymentStore, paymentMethodDefault } from "@/lib/admin-store";
import { Card, Field, Input, Select, Toggle, Button } from "@/components/admin/ui";
import { paymentMethodDefaultUnused as _unused } from "@/lib/admin-store" /* placeholder */;
